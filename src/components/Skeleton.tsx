import classNames from 'classnames';interface SkeletonProps {  times: number,  extraClassName?: string}const Skeleton = ({ times, extraClassName }: SkeletonProps) => {  const outerClassNames = classNames(    'relative',    'overflow-hidden',    'bg-gray-200',    'rounded',    'mb-2.5',    extraClassName  );  const innerClassName = classNames(    'animate-shimmer',    'absolute',    'inset-0',    '-translate-x-full',    'bg-gradient-to-r',    'from-gray-200',    'via-white',    'to-gray-200'  );  return (    <>      {Array(times)        .fill(0)        .map((_, i) =>               <div className={outerClassNames} key={i}>                 <div className={innerClassName}/>               </div>)}    </>  )}export default Skeleton;