import { Album } from '../../store/apis/albumsApi';import { useAddPhotoMutation, useFetchPhotosQuery } from '../../store';import Button from '../Button';import Skeleton from '../Skeleton';import PhotosListItem from './PhotosListItem';interface PhotosListProps {  album: Album;}const PhotosList = ({ album }: PhotosListProps) => {  const { data, isFetching, error } = useFetchPhotosQuery(album);  const [addPhoto, photoResults  ] = useAddPhotoMutation();  let content;  if(isFetching) {    content = <Skeleton extraClassName='h-8 w-8' times={4} />  } else if(error) {    content = <div>Error fetching photos...</div>  } else {    content = data?.map(photo => {      return <PhotosListItem key={photo.id} photo={photo} />    })  }  const handleAddPhoto = () => addPhoto(album);  return (    <div>      <div className='m-2 flex flex-row items-center justify-between'>        <h3 className='text-lg font-bold'>Photos in {album.title}</h3>        <Button loading={photoResults.isLoading} onClick={handleAddPhoto} primary>          + Add Photo        </Button>      </div>      <div className='mx-8 flex flex-row flex-wrap justify-center'>{content}</div>    </div>  )}export default PhotosList;